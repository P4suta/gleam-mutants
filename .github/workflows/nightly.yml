# SPDX-FileCopyrightText: 2026 gleam_mutants contributors
# SPDX-License-Identifier: MIT OR Apache-2.0

---

name: Nightly matrix and fuzz

"on":
  schedule:
    - cron: "17 2 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: nightly-${{ github.workflow }}
  cancel-in-progress: true

jobs:
  runtime-matrix:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        host: [erlang, node, deno, bun]
        target: [erlang, node, deno, bun]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: ./.github/actions/gleam-deps
      - run: >-
          node scripts/runtime-cell.mjs
          ${{ matrix.host }} ${{ matrix.target }}

  property-fuzz:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: ./.github/actions/gleam-deps
      - run: node scripts/property-fuzz.mjs
        env:
          GLEAM_MUTANTS_FUZZ_SECONDS: "900"
